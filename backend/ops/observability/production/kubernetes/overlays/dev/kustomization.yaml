apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../
  - dev-secrets.template.yaml

patchesStrategicMerge:
  - prometheus-dev-patch.yaml
  - grafana-dev-patch.yaml
  - ingress-dev-patch.yaml
